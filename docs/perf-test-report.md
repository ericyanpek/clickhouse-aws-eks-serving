# ClickHouse on EKS —— 性能测试报告

**中文** · [English](./perf-test-report.en.md)

> 测试日期:2026-07-05
> 数据集:ClickBench `hits`(Yandex Metrica 网页分析,99,997,497 行,单张 105 列宽表)
> 结果摘要:**在 2×i8g.4xlarge(本地 NVMe)的 1 分片 × 2 副本拓扑上,43 条 ClickBench 查询 best-of-3 总耗时 18.76s、平均 0.436s、中位数 0.175s;读 QPS 随查询重量差 1–2 个数量级(全表扫聚合 ~48、命中索引点查 ~2,780),并发从 1 增至 2 时 QPS 为 2.0×;重扫描查询使用 `parallel_replicas` 后最高提速 1.8×;单副本 Pod 故障期间服务保持可用、QPS 降约 50%、副本约 65s 恢复。**
>
> **状态说明:**这是历史 1×2 环境的实测记录,必须保留原始口径。当前代码目标是 1×3;在完成 3 副本复测前,不得把本文的线性外推写成 1×3 实测结论。当前文档权威级别见 [`docs/README.md`](./README.md)。

---

## 1. 测试环境

| 项 | 配置 |
|---|---|
| 平台 | Amazon EKS 1.34(平台 eks.27),us-east-1 |
| ClickHouse | 25.3.14.14,Altinity operator 0.27.1 |
| 拓扑 | **1 shard × 2 replica**(ReplicatedMergeTree),3 节点 ClickHouse Keeper |
| 数据节点 | 2 × **i8g.4xlarge**(16 vCPU / 128 GiB / Graviton4),各 1 Pod,跨 AZ(1a/1b) |
| 存储 | **本地 NVMe instance-store ~3.75TB**(非 EBS),local-static-provisioner |
| Keeper | 3 × m7g.large(非突发 Graviton),跨 3 AZ,gp3 |
| 压测端 | 独立 c7g.2xlarge(`system-bench` 节点,污点隔离,不与 ClickHouse 争资源) |
| 访问 | ClusterIP service `clickhouse-ch`(集群内轮询 2 副本) |
| 资源模型 | CPU request 14 / 无 limit;内存 request==limit 110Gi;`max_server_memory_usage_to_ram_ratio=0.9` |

> 说明:因 i8g.4xlarge 在 us-east-1 现货容量波动,当前为 **2 副本**(目标 3);压测结论按 2 副本口径给出。文中对 3 副本的数值仅为线性外推,不是实测结果。

### 数据集与存储效率

| 指标 | 值 |
|---|---|
| 行数 | 99,997,497(~1 亿) |
| 未压缩 | 50.62 GiB |
| 落盘(压缩后) | **13.45 GiB** |
| **压缩比** | **3.8×** |
| 数据落盘位置 | `/var/lib/clickhouse/`(本地 NVMe)|
| 复制延迟(加载后) | 0 |

---

## 2. 阶段一:单节点查询延迟(43 条 ClickBench 查询)

每条查询跑 3 次取最优(ClickBench 标准的热运行口径),单连接(并发=1)。

### 汇总

| 指标 | 值 |
|---|---|
| 查询总数 | 43 |
| 总耗时(best 累加) | **18.76s** |
| 平均 | 0.436s |
| 中位数(p50) | 0.175s |
| p90 | 1.268s |
| 最快(Q1 `COUNT(*)`) | 0.002s |
| 最慢(Q29) | 5.211s |

### 分布特征

- **约 65% 的查询在亚秒级**(<0.3s),点查/前导列过滤类查询命中稀疏索引,毫秒级返回。
- **少数扫描密集型查询受到单节点算力限制**(见下),它们扫描大范围列并执行高基数聚合,主要受单节点 CPU 约束。

### 最慢的 5 条(单节点扫描天花板)

| 查询 | best(s) | 特征 |
|---|---|---|
| Q29 | 5.211 | 大范围 URL/正则相关重聚合 |
| Q19 | 1.665 | 高基数 GROUP BY |
| Q24 | 1.347 | 多列扫描聚合 |
| Q34 | 1.284 | 宽扫描 + 排序 |
| Q35 | 1.268 | 宽扫描 + 排序 |

> 完整逐条数据见 [`docs/perf-results/clickbench-43queries.csv`](./perf-results/clickbench-43queries.csv)。

---

## 3. 阶段二:parallel_replicas 单查询加速(重查询)

在最慢的查询上开启 `parallel_replicas`(2 副本并行扫同一份数据),验证「分片前先用 parallel_replicas 顶」的设计主张。

设置:`allow_experimental_parallel_reading_from_replicas=2, max_parallel_replicas=2, cluster_for_parallel_replicas='main'`

| 查询 | 关闭(s) | 开启(s) | 效果 |
|---|---|---|---|
| **Q24** | 1.337 | 0.737 | **1.81× 加速** ✅ |
| **Q29**(最重) | 5.269 | 3.958 | **1.33× 加速** ✅ |
| Q19 | 1.778 | 2.489 | 0.71×(变慢)⚠️ |
| Q33 | 1.232 | 2.669 | 0.46×(变慢)⚠️ |
| Q34 | 1.392 | 4.627 | 0.30×(变慢)⚠️ |

### 结果与适用条件

- **对大扫描/聚合查询(Q24/Q29)有效**:2 副本分摊扫描量,单查询提速 1.3–1.8×。这验证了在撞到「单查询算力天花板」时,`parallel_replicas` 能把分片的引入再往后推。
- **对轻量 / 带 LIMIT / 协调开销敏感的查询反而变慢**:并行协调的固定开销 > 扫描收益。
- **实践建议**:`parallel_replicas` 应**按查询选择性开启**(针对已知的重扫描查询),而非全局默认打开。

---

## 4. 阶段三:读并发 / QPS 扩展

> **指标口径：** QPS 取决于查询复杂度,不是集群的固定属性。本节 4.1 使用一条全表扫描聚合查询测量副本读扩展,因此 QPS 约为几十;4.2 记录不同类型查询的 QPS,其中点查可达数千。4.1 的 ~52 QPS 只适用于该全表扫描查询,不能作为其他查询类型的吞吐上限。

### 4.1 读扩展线性度(用全表扫描重查询)

从独立的 bench 节点用 `clickhouse-benchmark` 打 ClusterIP service(轮询 2 副本),查询为全表 `GROUP BY RegionID`(**每次扫全部 1 亿行**的重聚合),并发阶梯 1→16,每档稳态 QPS:

| 并发 | 稳态 QPS | 相对 c=1 | 说明 |
|---|---|---|---|
| 1 | ~22.5 | 1.0× | 单连接基线 |
| 2 | ~44.8 | **2.0×** | 并发 1→2 时 QPS 同比增加 |
| 4 | ~50.5 | 2.2× | 开始饱和 |
| 8 | ~52 | 2.3× | 饱和 |
| 16 | ~52 | 2.3× | 饱和(无进一步提升) |

峰值扫描吞吐:**~50 亿行/秒、~20 GiB/s**(RPS/带宽,本地 NVMe + 向量化扫描)。

**读扩展结论:**

- **并发 1→2 时 QPS 为 2.0×**:本轮结果与 ClusterIP 将负载分散到 2 个副本的预期一致。
- **4 并发后饱和于 ~52 QPS**:因为测试查询是全表扫描聚合(每查询扫 1 亿行),**2 台 i8g 的 CPU 在约 4 并发即吃满**——这正是「读吞吐上限 = 副本数 × 单节点并发能力」的体现。对这种重扫描负载,提升 QPS 上限的手段是**加副本**(线性)。
- 峰值扫描吞吐为 **~50 亿行/秒、~20 GiB/s**。QPS 约为 52 的主要原因是每个查询都扫描约 1 亿行,因此需要同时报告 QPS 和 RPS。
- 外推:3 副本时该饱和 QPS 预期约 ~75(线性外推)。

### 4.2 不同查询类型的真实 QPS(并发 8)

同一集群,QPS 随查询重量差 1–2 个数量级。真实 BI/serving 负载几乎都带过滤(时间范围、维度),命中 ClickHouse 稀疏主键索引(本表主键 `CounterID, EventDate, ...`),只扫一小部分数据,QPS 远高于全表扫描。实测:

| 查询类型 | QPS | RPS(每秒扫描行数) | 说明 |
|---|---|---|---|
| **纯连接开销**(`SELECT 1`,无扫描) | **~5,860** | — | 集群请求处理上限 |
| **点查**(`WHERE CounterID=?`,命中主键索引) | **~2,780** | 4,400 万 | 只扫该 key 的数据块 —— 比全表扫高 ~58× |
| **带过滤的聚合**(命中索引后 GROUP BY,典型 BI) | **~370** | 31.5 亿 | 生产 serving 负载的典型区间 |
| **全表聚合**(§4.1 的重查询,对照) | **~48** | 48 亿 | 每次扫全部 1 亿行 |

> 原始日志:[`docs/perf-results/qps-by-query-type.txt`](./perf-results/qps-by-query-type.txt)

**关键结论:**

1. **QPS 由查询复杂度决定,不是集群固定属性**:同一集群,查询从「全表扫」变「命中索引」,QPS 从 ~48 跳到 ~2,780(**58×**)。
2. **RPS(每秒扫描行数)用于补充解释硬件扫描能力**:全表聚合为 48 QPS,对应 48 亿行/秒;较低的 QPS 与单查询扫描量有关。
3. **本轮点查和带过滤聚合为几百到数千 QPS**,全表扫描为几十 QPS。选型和容量规划应使用目标查询的实际形态,不能用全表扫描结果代表其他负载。

---

## 5. 阶段四:HA 混沌测试(读负载下杀副本)

在持续读负载(并发 4,`--continue_on_errors`)下,杀掉一个副本 Pod `chi-ch-main-0-1-0`,观察服务连续性与恢复。

| 阶段 | QPS | 错误 | 服务状态 |
|---|---|---|---|
| 杀之前(2 副本) | ~47–48 | 0 | 正常 |
| **杀瞬间** | — | **仅 1 个**(in-flight 查询) | 不中断 |
| 杀之后(1 副本) | ~25–26 | 0(后续) | **持续可用** |
| 恢复后 | 回升 | 0 | 2 副本 |

- **副本自愈时间:约 65 秒**(Pod 重建 + 本地 NVMe 上数据仍在,快速恢复)。
- 注:本次是 **Pod 被杀、节点存活**,本地 NVMe 数据卷还在,故恢复极快。若是**节点整体丢失**(本地盘随之丢失),则需从存活副本全量重建(分钟级,取决于数据量),这是本地 NVMe 方案的固有取舍。

### 结论

本轮 Pod 删除测试观测到:**单副本故障后 QPS 降约 50%(2→1),服务保持可用,出现 1 个瞬时查询错误,副本约 65s 恢复**。该结果与 ReplicatedMergeTree 的多主对等复制机制一致。节点与本地盘同时丢失的恢复不在本轮测试范围内。

---

## 6. 总体结论

| 维度 | 结果 | 印证的设计主张 |
|---|---|---|
| 单节点查询性能 | 43 查询平均 0.436s,65% 亚秒 | 该硬件与数据规模下的宽表 OLAP 基线 |
| 压缩效率 | 3.8× | 列存 + 压缩 |
| 单查询天花板 | 最慢 5.2s(Q29),重扫描受单节点 CPU 限 | 「scale-up first」——单节点能力有天花板 |
| parallel_replicas | 重查询 1.3–1.8× 加速(轻查询反而慢) | 「分片前先用 parallel_replicas 顶」——对重查询成立,需选择性开启 |
| 读扩展 | 并发 1→2 时 QPS 为 2.0× | 本轮结果支持两副本分散读取 |
| QPS(随查询重量) | 全表扫 ~48 → 带过滤聚合 ~370 → 点查 ~2,780 → 空查询 ~5,860 | QPS 是查询复杂度的函数,非集群固定值 |
| 读吞吐上限 | 重扫描负载下 ~4 并发饱和于单节点 CPU;峰值 ~50 亿行/s 扫描 | 「读吞吐 = 副本数 × 单节点并发」 |
| HA | 单副本故障不中断,QPS 降 50%,~65s 自愈 | 「多主对等,挂一个其余照常」 |

### 与设计定位的呼应

本轮压测为以下设计选择提供了 1×2 环境下的实测数据(详见 [`docs/community-corroboration.md`](./community-corroboration.md)):**1 分片垂直扩容、本地 NVMe、通过副本分散读取、对重查询选择性使用 parallel_replicas**。数据同时记录了 `parallel_replicas` 使部分轻查询变慢的结果。是否扩展到 1×3 或其他负载,仍需相应复测。

### 后续可深化(本轮未覆盖)

- **写入 / merge 吞吐压测**(用 NYC Taxi 数据集)——本轮聚焦读,未压写。
- **3 副本口径复测**——当前 2 副本(i8g 容量所限),补齐第 3 副本后可验证 QPS 线性外推。
- **节点级故障恢复计时**——本轮是 Pod 级(数据卷存活);节点整体丢失 + 本地盘丢失的全量重建时间未测。

---

## 附:原始数据

- 43 查询逐条延迟:[`docs/perf-results/clickbench-43queries.csv`](./perf-results/clickbench-43queries.csv)
- 测试脚本与拓扑:见仓库 `manifests/` 与 `scripts/`
